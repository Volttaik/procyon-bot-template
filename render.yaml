services:
  - type: web
    name: procyon-bot
    env: node
    plan: starter
    buildCommand: npm install --production
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
